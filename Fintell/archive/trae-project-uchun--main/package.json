{
  "name": "trae-project-uchun",
  "private": true,
  "packageManager": "npm@10.5.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start --",
    "lint": "turbo run lint",
    "db:migrate": "tsx apps/api/migrate.ts"
  },
  "devDependencies": {
    "@types/pg": "^8.18.0",
    "turbo": "latest"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "postgres": "^3.4.8",
    "tsx": "^4.11.0"
  }
}
